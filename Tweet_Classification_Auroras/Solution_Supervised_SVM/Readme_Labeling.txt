 For labeling the tweets: 

(Original tweets are in ../Data)

1. dispel4py simple labeling.py  -d '{"read" : [ {"input" : "tweets_5000_page1.json"} ]}'
2. Label manually the tweets opening the output file(../Data/labeled_data.json") of the previous program
and store them in ../Label_Data
